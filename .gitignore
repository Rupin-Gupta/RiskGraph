# Secrets
.env

# Python
.venv/
__pycache__/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Local tooling
.DS_Store
graphify-out/

# Generated risk runs (run.json per date, backtest history)
data/runs/

# Downloaded Basel Framework HTML (fetched by corpus/download.py, not committed)
corpus/raw/

# Written on the deployed host (basic auth file, TLS certificate, ACME webroot)
/htpasswd
/certs/
/certbot-www/
